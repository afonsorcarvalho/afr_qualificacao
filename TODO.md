# TODO — afr_qualificacao

## Em curso
- <nada>

## Pendente — outros
- F4.1 (16.0.3.4.x): **Dashboard/lista de Padrões Vencidos** — view dedicada (tree+kanban) sobre `engc.calibration.instruments` com filtros: `Vencidos`, `Vence em 30 dias`, `Sem certificado`. Menu em Qualificações → Configurações. Decoração vermelha/amarela na tree. Computed `days_to_expiration` + `expiration_status` (selection: valid/warning/expired/none).
- F4.2 (16.0.3.4.x): **Cron diário aviso de vencimento** — `ir.cron` em `afr_qualificacao` roda 1×/dia (ex: 07:00), procura instrumentos com `validate_calibration` ∈ {hoje, hoje+7, hoje+30 dias}, envia `mail.activity` ao(s) gestor(es) do grupo `group_afr_qualificacao_manager` + opcionalmente email template. Config param: dias antecedência (default 30/7/0), ligar/desligar cron, lista emails extras.
- F4.5 (16.0.3.4.x): **Calendar popup OS com instrumentos** — no popup do calendar (event_open_popup) adicionar lista resumida dos instrumentos padrão agregados via `qualificacao_ids.standard_instrument_ids`. Considerar HTML compute para preview.
- F4.6 (16.0.3.4.x): **Cancelar OS reverte qualifs + reabre relatórios** — `afr.qualificacao.os.action_cancel`: cascatear `state=cancelled` nas qualifs (não destruir certificados, só marcar cancelled); reabrir `afr.qualificacao.os.relatorio` voltando para state 'in_progress' (ou flag específico). Adicionar warning de confirmação. Testar reversão idempotente.
- F5 (16.0.3.4.0): reports completos + record rules technician + **flag `qualif_block_approval_without_collects`** em `res.config.settings` (default False = warning não-bloqueante atual; True = bloqueia `action_request_approval` se há collect.items required pending)
- F6 (16.0.3.5.0): relatório qualificação PDF/DOCX baseado em template Word com tags — render fields Odoo + anexar no final: fotos, PDFs, certificados qualificadores+padrões, tabelas de dados coletados, gráficos com curvas dos parâmetros (cliente recebe arquivo editável)
- Avaliar QR code de aceite digital (cliente assina no portal) — fase futura
- Avaliar multi-idioma (PT/EN) no relatório — se cliente internacional aparecer
- Avaliar capa com imagem de fundo / branding personalizado

## Feito
- 2026-05-18 — F4.9 (16.0.3.4.0): ciclo QD vira wrapper de coletas. `afr.qualificacao.cycle.collect_item_ids` (O2M via cycle_id inverse) + computed `collects_total`/`collects_done`/`collects_progress`/`collects_complete`. Helper `_sync_state_from_collects`: auto-transição pending→passed quando todas required collected (relatorio_id propagado do último collect; executed_date hoje). state=failed respeitado (não sobrescreve manual). `collect.item.write/create` triggers `_sync_state_from_collects` no cycle pai. Seção "Ciclos QD" removida da aba Coletas do relatório (redundante — view só anexos + malhas). Tab Ciclos no form qualif mostra progresso (3/5) + toggle Completo + state badge. 130 tests PASS.
- 2026-05-18 — F4.8 (16.0.3.4.0): ciclos QD + malhas Calibração também via relatório. `relatorio_id` (M2O) + `collected_without_relatorio` computed em `afr.qualificacao.cycle` e `afr.qualificacao.malha`. Onchange/write auto-link via `default_relatorio_id` no contexto. Gate hard em `qualif.action_mark_approved`: ValidationError se cycle/malha state in (passed/failed) sem `relatorio_id`. Tabs novas no relatório form: "Ciclos (QD)" + "Malhas (Calibração)" com tree readonly + botão "Coletar" inline (action_open_for_collect com `attach_to_relatorio_id`). Form views novas para cycle/malha em `views/qualificacao_subrecords_views.xml`. Action `action_create_engc_calibration` no malha cria engc.calibration auto-populada (client/equipment da qualif, technician do OS, instruments dos padrões, procedure default). Fixtures de testes adaptadas (helper `_get_relatorio` em common.py). 130 tests PASS.
- 2026-05-17 — F4.7 (16.0.3.4.0): coletas só via relatório. `collect.item.collected_without_relatorio` computed + alert amarelo no form. `afr.qualificacao.os.relatorio` ganhou O2M `collect_item_ids` + computed `pending_collect_item_ids` (filtrado por `os_id`). Pages novas no relatorio form: "Coletas Realizadas" + "Coletas Pendentes da OS". Gate hard em `action_mark_approved` (sem flag): `ValidationError` se collect.items required+collected sem `relatorio_id`. 4 tests novos (130 total PASS).
- 2026-05-17 — F4.3 (16.0.3.4.0): casamento instrumento ↔ grandeza. Inherit `engc.calibration.measurement.unit` com `afr_sensor_kind_id`. Computed `sensor_kind_ids`/`measurement_unit_ids` derivados de `certificate_ids.uncertainty_lines.unit_of_measurement` nos instruments. `procedimento.item` ganhou `requires_instrument` (default por kind: foto/pdf/outro=False; excel/qualificador_data=True) + `required_sensor_kind_ids`. `collect.item` related + computed `coverage_complete`/`coverage_warning_text`/`missing_sensor_kind_ids`. Agregação no qualif + gate em `action_mark_approved` lendo nova flag `qualif_block_approval_incomplete_coverage`. Painel "Cobertura Metrológica" no form instrumento + alert vermelho coverage incompleta. 16 tests novos PASS.
- 2026-05-17 — F4.4 (16.0.3.3.1): view `<calendar>` em `afr.qualificacao.os` com `date_planned_start`/`date_planned_end` + color por `tecnico_default_id`. Action `view_mode="tree,kanban,calendar,form"`.
- 2026-05-17 — Conversão `addons/afr_qualificacao` em git submodule (repo público `github.com/afonsorcarvalho/afr_qualificacao`, branch main). Histórico 124 commits preservado via subtree split. Backup tag `pre-afr-qualificacao-submodule-conversion`. Monorepo commit `717d1c8`.
- 2026-05-17 — F4 (16.0.3.3.0): padrões metrológicos M2M `engc.calibration.instruments` em `collect.item` + agregação computed em `afr.qualificacao` (union) + `standards_all_valid`/`warning_text` baseados em `validate_calibration >= today`. Flag `qualif_block_approval_expired_standards` em `res.config.settings` (default False = warning chatter; True = `ValidationError` em `action_mark_approved`). Tab "Padrões Metrológicos" no form qualif + collect_item com alert quando expirado. 11 tests novos.
- 2026-05-17 — Trunk renomeada `feat/ciclo-report-materiais-pagina-separada` → `main-monorepo` (default branch GitHub atualizado via `gh repo edit`; master legado preservado intocado; memórias atualizadas)
- 2026-05-17 — F3 batch 16.0.3.2.1 commit 82d5578 PUSHED: tracking procedimento + chatter (F3.1), reorder stat buttons Pendentes/Coletadas + Aprovadas só visível >0 (F3.2), tree collect.items sem editable=bottom (F3.3), `ir.module.category` Qualificações + grupos com category_id (F3.4), menu raiz com grupo Técnico (F3.5), wizard descrição width full via separator (F3.6), `name_get` cycle/malha (F3.7), labels "cobertos"→"realizados" + "Cobertura técnica"→"Execução" (F3.8), domain `cycle_ids`/`malha_ids` filtra por `qualificacao_id.os_id` (F3.9). Smoke test 117/117 PASS.
- 2026-05-17 — F3 (16.0.3.2.0): procedimento template + collect.items unificados + wizard apply_procedimento (commit bba0a0d)
- 2026-05-16 — Fontes PDF +30% (parágrafos e tabelas) p/ melhor legibilidade
- 2026-05-16 — Painel HTML `qualif_subtotals_html` no form SO (subtotal por equipamento)
- 2026-05-16 — Section lines por equipamento no SO (display_type='line_section' via wizard apply)
- 2026-05-16 — PDF escopo usa `line.name` (descrição Sales) em vez de nome produto
- 2026-05-16 — Fix views: notebook+page para text/html fields (memória `feedback_odoo_text_field_width`)
- 2026-05-16 — Fix views: group nesting para renderizar label (memória `feedback_odoo_group_label`)
- 2026-05-16 — Modelo `afr.qualificacao.standard` + M2M cycle_type/malha_type + 8 normas seed (ISO 17025, FDA 21 CFR Part 11, ANVISA RDC 658/665, ISPE GAMP 5, WHO TRS 961, VIM, ABNT NBR ISO/IEC 17025)
- 2026-05-16 — Template QWeb dedicado `quotation_template.xml` (capa+sumário+descritivo+escopo+normas+resumo+condições+aceite) inherit condicional `sale.report_saleorder_document`
- 2026-05-16 — Helpers sale.order: `has_qualif_lines`, `qualif_standard_ids`, `_qualif_equipment_summary`, `_qualif_type_descriptions`
- 2026-05-16 — 13 testes novos em `test_quotation_report.py` (agregação, fallback, render PDF/HTML) — 43/43 pass
- 2026-05-16 — Bump versão 16.0.2.1.0 → 16.0.2.2.0
