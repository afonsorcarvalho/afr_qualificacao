# TODO — afr_qualificacao

## Em curso
- <nada>

## Pendente — outros
- F4.1 (16.0.3.4.x): **Dashboard/lista de Padrões Vencidos** — view dedicada (tree+kanban) sobre `engc.calibration.instruments` com filtros: `Vencidos`, `Vence em 30 dias`, `Sem certificado`. Menu em Qualificações → Configurações. Decoração vermelha/amarela na tree. Computed `days_to_expiration` + `expiration_status` (selection: valid/warning/expired/none).
- F4.2 (16.0.3.4.x): **Cron diário aviso de vencimento** — `ir.cron` em `afr_qualificacao` roda 1×/dia (ex: 07:00), procura instrumentos com `validate_calibration` ∈ {hoje, hoje+7, hoje+30 dias}, envia `mail.activity` ao(s) gestor(es) do grupo `group_afr_qualificacao_manager` + opcionalmente email template. Config param: dias antecedência (default 30/7/0), ligar/desligar cron, lista emails extras.
- F4.5 (16.0.3.4.x): **Calendar popup OS com instrumentos** — no popup do calendar (event_open_popup) adicionar lista resumida dos instrumentos padrão agregados via `qualificacao_ids.standard_instrument_ids`. Considerar HTML compute para preview.
- F4.6 (16.0.3.4.x): **Cancelar OS reverte qualifs + reabre relatórios** — `afr.qualificacao.os.action_cancel`: cascatear `state=cancelled` nas qualifs (não destruir certificados, só marcar cancelled); reabrir `afr.qualificacao.os.relatorio` voltando para state 'in_progress' (ou flag específico). Adicionar warning de confirmação. Testar reversão idempotente.
- F5 (16.0.3.4.0): reports completos + record rules technician + **flag `qualif_block_approval_without_collects`** em `res.config.settings` (default False = warning não-bloqueante atual; True = bloqueia `action_request_approval` se há collect.items required pending)
- F6 (16.0.3.5.0): relatório qualificação PDF/DOCX baseado em template Word com tags — render fields Odoo + anexar no final: fotos, PDFs, certificados qualificadores+padrões, tabelas de dados coletados, gráficos com curvas dos parâmetros (cliente recebe arquivo editável)
- Avaliar QR code de aceite digital (cliente assina no portal) — fase futura
- Avaliar multi-idioma (PT/EN) no relatório — se cliente internacional aparecer
- Avaliar capa com imagem de fundo / branding personalizado

## Feito
- 2026-05-25 — **F9.4 (16.0.4.19.0): cotação online LabQuali + email brand + post_init_hook seed.**
  - **F9.4 (cotação online portal)**: `views/sale_order_portal_template.xml` herda `sale.sale_order_portal_content`, esconde conteúdo padrão (xpath attribute `t-if="not has_qualif_lines"` nos 2 divs) e insere layout LabQuali via `t-call` quando `has_qualif_lines=True`. Conteúdo `sale_order_online_qualif_content` espelha PDF: capa com logo+faixa azul/laranja+meta box, TOC clicável (anchors nativos `#lq-anchor-N`), loop em blocos com mesma dispatch (`static/equipment_scope/cycle_specs/schedule/standards_table/financial/optionals/acceptance`), numeração + indent filho. Variável `sale_order` (não `doc` como PDF). SCSS novo `static/src/scss/sale_portal_qualif.scss` (paleta vars, responsivo mobile, scroll-behavior smooth), registrado via `"assets": {"web.assets_frontend": [...]}` no manifest (Odoo 16 way). Botões nativos sidebar (Aceitar/Assinar/Pagar) intactos — template pai não tocado.
  - **F9.3 (email LabQuali)**: nova `mail.template` `email_template_proposal_labquali` em `data/mail_template_proposal.xml` — layout HTML inline email-safe (paleta brand, header com logo+faixa, card cinza com Nº/data/validade, CTA portal, rodapé). Override `_find_mail_template` em `sale_order.py`: SO com `has_qualif_lines` + state draft/sent → retorna template LabQuali; demais SOs caem no super(). Wizard "Enviar por Email" pré-seleciona automaticamente. PDF anexado.
  - **post_init_hook**: `hooks.py` novo — `_install_proposal_template_seed(cr, registry)` cria template default "Proposta LabQuali QI/QO/QD" via ORM APENAS no install (não em updates). Idempotente (skip se xmlid já existe). Registra `ir.model.data` com `noupdate=True` para env.ref() em tests. 2-pass: cria linhas + propaga `parent_id`. `proposal_template_seed.xml` removido de `data/` do manifest. Resolve bug: linhas eliminadas pelo utilizador NÃO voltam em updates.
  - **UX polimentos editor template**: `display_label` (Char compute: cascade title→section.name→kind label) + `display_number` (Char compute: numeração hierárquica preview) em `AfrProposalTemplateLine` e `AfrProposalBlock`. Colunas "Nº" + "Conteúdo" na tree do template + tab SO. Override `_name_search` em ambos modelos para busca por título/section.name/kind label (popup parent_id agora mostra nomes, não IDs). Tree standalone para popup "Pesquisar mais" (`view_proposal_template_line_tree`, `view_proposal_block_tree`).
  - **Botão Editar no template**: `action_edit_template_line` em `AfrProposalTemplateLine` — abre seção (static) ou form da linha (dinâmico). Form view `view_proposal_template_line_form` com save+cancel buttons. `view_proposal_block_form` ganha botão "Salvar e Fechar" (fix: `special="cancel"` antigo descartava edições).
  - **Toggle `show_title`** (default True): novo Boolean em block + template line — desmarcar omite título no PDF (renderiza só conteúdo). Coluna "Titulado" na tree.
  - **TOC fix wkhtmltopdf**: âncoras vazias `<a name>` removidas por WebKit. Solução: conteúdo `&#160;` (nbsp) preserva. Script JS inline no template remove `<base href>` injetado por `web.minimal_layout` APÓS imgs/css carregarem (`window.addEventListener('load')`) — anchors `#X` viram links internos PDF. Pré-resolve `img.src/link.href/a[non-fragment].href` para absolutos antes de remover base. Refs: drupal.org/project/print/issues/1313754, github wkhtmltopdf/wkhtmltopdf#3486.
  - **Diferenciação visual pai/filho no PDF**: `.qq-section-title.qq-section-title-child` (selector encadeado, especificidade 0,0,2,0 — supera ordem do CSS). Pai UPPERCASE bold 18px brand + faixa 3px. Filho normal bold 15px brand-2 + faixa fina 1px cinza. Mesma hierarquia visual no TOC.
  - **Logo LabQuali**: `external_layout_labquali.xml` max-height 80→100px, max-width 380→460px.
- 2026-05-25 — **F9.2 (16.0.4.18.0): hierarquia de blocos + numeração + índice clicável no PDF.**
  - **F9.1**: `afr.proposal.block` ganha `parent_id`/`child_ids`/`show_number`/`show_title`. `afr.proposal.template.line` espelha (`parent_id`/`show_number`/`show_title` + computed `display_label`/`display_number` para preview no editor). Constraint anti-ciclo. `SaleOrder._proposal_block_numbering()` retorna `{block_id: 'N'|'N.M'|''}` com algoritmo de contadores por pai (suporta profundidade arbitrária). `_seed_proposal_blocks()` 2-pass propaga parent das linhas do template aos blocos. Report QWeb prefixa títulos com número (`3 — Qualificação Térmica`, `3.1 — QI`) + CSS `.qq-section-title-child` (indent 16px + font menor) + skip título quando `show_title=False`. Seeds: nova secção `proposal_section_qualif_termica` + nova linha `proposal_template_labquali_l02b` (Qualificação Térmica) com QI/QO/QD como filhos.
  - **F9.2 (TOC)**: Índice clicável após a capa lista blocos `included AND show_title`, números + indent para filhos. Anchor target via `<a name="qq-anchor-#{id}">&#160;</a>` (WebKit remove âncoras vazias). **Fix crítico**: `web.minimal_layout` injeta `<base href>` que faz wkhtmltopdf resolver `#anchor` como URL absoluta. Solução: `<script>` inline no template remove o `<base>` em runtime (executa durante `--javascript-delay`), permitindo wkhtmltopdf criar link interno do PDF. Refs: drupal.org/project/print/issues/1313754, github.com/wkhtmltopdf/wkhtmltopdf/issues/3486. CSS TOC com formatação de título (UPPERCASE bold, sem visual de link).
  - **Views**: editor do template (`proposal_template_views.xml`) ganha colunas `Nº` (preview), `Numerado`, `Titulado`, `Linha Pai`, `Conteúdo` (cascade title→section→kind). Tree standalone `view_proposal_template_line_tree` para popup "Pesquisar mais". SO tab "Proposta (Blocos)" reflete colunas equivalentes.
  - **Outros**: capa ganha imagem default `static/img/cover_default.jpg` (collage equipamentos, full-bleed width).
- 2026-05-24 — F8.16 (16.0.4.15.0): Equipment Scope sem tabelas inline QO/QD (dados duplicados removidos — tabela completa só em "Tabela Resumo de Ciclos"). Bullets QO/QD exibem `ciclo — qtd: N — temperatura — tempo esteril`. `_qualif_equipment_summary` adiciona `temperature`+`duration` aos items dict (cycle_type). Labels `QUALIF_TYPE_LABELS` padronizadas sem abreviação (ex: "Qualificação de Operação"). cycle_specs default title → "Tabela Resumo de Ciclos". CSS `.qq-type-block` margin 6→14px (mais respiro entre seções). 1 test adaptado (`test_report_renders_qo_in_equipment_scope` substitui `test_report_renders_qo_table_inline_in_equipment_scope`). 198/0/0 PASS.
- 2026-05-24 — F8.15 (16.0.4.14.0): Escopo + Tabela Resumo refinados. Tabelas QO/QD inline + cycle_specs reordenam cols: **Ciclo | Qtd** (era Qtd | Ciclo) + col **Tempo** renomeada "Tempo Esteril" + nova col **Tempo Estimado** (= `row.estimated_hours_total`). `_qualif_cycle_rows_for` + `_qualif_cycle_specs` adicionam `estimated_hours_total` (cycle.estimated_hours × qty) em cada row. `tfoot` mostra `Total: <qty> ciclo(s)` (sum de qty, independente do tipo) + horas/dias. Ordem seções no Equipment Scope reordenada via `QUALIF_TYPE_LABELS`: QI → **Calibração** → QO → QD → QS (Calib antes do QD agora). 198/0/0 PASS (test `test_equipment_summary_type_order_follows_selection` adaptado).
- 2026-05-23 — F8.14 (16.0.4.13.0): cronograma estimado. Campo `estimated_hours` (Float) em cycle_type + malha_type + type.config + template lines (QD/QO/Calib) + configurator sublines (incl bulk) + sale.order.line. Onchange/autofill/apply cascade. Helpers em sale.order: `_qualif_estimated_hours(equipment=None)`, `_qualif_estimated_days(...)` (/8), `_qualif_schedule_rows()`, `_qualif_section_hours(equip, phase)`. PDF: `<tfoot>` em tabelas QO/QD/cycle_specs (corrige rodapé faltante + breakdown), rodapé "Cronograma estimado: N dias úteis (M horas)" no Equipment Scope, novo block_kind='schedule' (template-opcional) com tabela equipamento×horas×dias e total geral. Seeds iniciais nos cycle/malha types. 11 tests novos (8 estimated_hours + 3 proposal_report). 236/0/0 PASS.
- 2026-05-23 — F8.13.1 (16.0.4.12.1): 3 baselines F8.6-F8.10 corrigidas — **225/0/0 PASS**. `test_seed_default_template_loaded` espera 17 (cycle_specs removido na F8.8 — comentário roadmap). `test_snapshot_cycle_specs_reflects_temperature` (test_proposal_block_edit) e `test_render_cycle_specs_block` (test_proposal_report) ganham `setUpClass` que injeta linha `cycle_specs` (title="Tabela de Ciclos") no template `proposal_template_labquali` dinamicamente. Bloco cycle_specs continua disponível como tipo, só não está no seed default.
- 2026-05-23 — F8.13 (16.0.4.12.0): `description` (Char, translate) por linha QD/QO/Calib em `afr.qualificacao.config.template.*`. Coluna no template form. Autofill `_onchange_config_template` usa `line.description or product.name` como default da subline do configurador. Cadastra descrição rica 1× no template, reaproveita em todas cotações.
- 2026-05-23 — F8.12 (16.0.4.11.0): cycle_type + malha_type ganham `default_unit_price` (Float). Onchange + autofill sublines usa `default_unit_price or product.list_price` como default do unit_price (corrige bug "preço vindo zerado" quando produto compartilhado tem list_price=0). Removido `do_qo` checkbox do equipment_line + bulk wizard (presença de `qo_line_ids` = aplicar QO; fallback type.config descontinuado). ACL `access_configurator_bulk_qo_user` adicionada (corrige Erro de Acesso "Bulk QO Line" ao clicar Adicionar Vários Equipamentos).
- 2026-05-23 — F8.11 (16.0.4.10.0): sublines configurador (QO/QD/Calib) ganham `description` + `unit_price` editáveis. Default vem de `product.name` / `product.list_price` via `_onchange_cycle_type_defaults` / `_onchange_malha_type_defaults`. Subtotal recompute `unit_price * qty`. Em `action_apply`, description vira `name` SO line (fallback product.name) e unit_price vira `price_unit` (fallback list_price). Bulk wizard "Adicionar Vários" ganha `qo_line_ids` + aba Ciclos QO + description/unit_price em todas sublines. Aba "Optional Products" do sale_management escondida via inherit. Template autofill (`_onchange_config_template`) propaga description+unit_price das sublines. action_duplicate idem.
- 2026-05-23 — F8.6-F8.10 batch (16.0.4.9.0) commit `d34f3cc` PUSHED + monorepo bump `4522e20`:
  - F8.6 (16.0.4.5.0): quebra de página por bloco (`page_break`).
  - F8.7 (16.0.4.6.0): configurador reordena passos; título editável no template.
  - F8.8 (16.0.4.7.0): escopo detalhado — ciclos QO sem carga; QO+QD inline no Equipment Scope; seed cycle_types.
  - F8.9 (16.0.4.8.0): seeds categorias equipamento + malhas + template "Pacote Autoclave a Vapor".
  - F8.10 (16.0.4.9.0): Equipment Scope sem subtotal por equipamento; calib renderiza "0N Calibração de <malha>"; CSS Resumo Financeiro uniformizado (font 14px, totals colados, labels right-aligned).
  - **Falhas baseline conhecidas (não regressionadas por F8.11):** `test_proposal_block_edit.test_snapshot_cycle_specs_reflects_temperature`, `test_proposal_catalog.test_seed_default_template_loaded` (count 17 vs 18 esperado), `test_proposal_report.test_render_cycle_specs_block` ("Tabela de Ciclos" não encontrado). A investigar.
- 2026-05-18 — F4.9 (16.0.3.4.0): ciclo QD vira wrapper de coletas. `afr.qualificacao.cycle.collect_item_ids` (O2M via cycle_id inverse) + computed `collects_total`/`collects_done`/`collects_progress`/`collects_complete`. Helper `_sync_state_from_collects`: auto-transição pending→passed quando todas required collected (relatorio_id propagado do último collect; executed_date hoje). state=failed respeitado (não sobrescreve manual). `collect.item.write/create` triggers `_sync_state_from_collects` no cycle pai. Seção "Ciclos QD" removida da aba Coletas do relatório (redundante — view só anexos + malhas). Tab Ciclos no form qualif mostra progresso (3/5) + toggle Completo + state badge. 130 tests PASS.
- 2026-05-18 — F4.8 (16.0.3.4.0): ciclos QD + malhas Calibração também via relatório. `relatorio_id` (M2O) + `collected_without_relatorio` computed em `afr.qualificacao.cycle` e `afr.qualificacao.malha`. Onchange/write auto-link via `default_relatorio_id` no contexto. Gate hard em `qualif.action_mark_approved`: ValidationError se cycle/malha state in (passed/failed) sem `relatorio_id`. Tabs novas no relatório form: "Ciclos (QD)" + "Malhas (Calibração)" com tree readonly + botão "Coletar" inline (action_open_for_collect com `attach_to_relatorio_id`). Form views novas para cycle/malha em `views/qualificacao_subrecords_views.xml`. Action `action_create_engc_calibration` no malha cria engc.calibration auto-populada (client/equipment da qualif, technician do OS, instruments dos padrões, procedure default). Fixtures de testes adaptadas (helper `_get_relatorio` em common.py). 130 tests PASS.
- 2026-05-17 — F4.7 (16.0.3.4.0): coletas só via relatório. `collect.item.collected_without_relatorio` computed + alert amarelo no form. `afr.qualificacao.os.relatorio` ganhou O2M `collect_item_ids` + computed `pending_collect_item_ids` (filtrado por `os_id`). Pages novas no relatorio form: "Coletas Realizadas" + "Coletas Pendentes da OS". Gate hard em `action_mark_approved` (sem flag): `ValidationError` se collect.items required+collected sem `relatorio_id`. 4 tests novos (130 total PASS).
- 2026-05-17 — F4.3 (16.0.3.4.0): casamento instrumento ↔ grandeza. Inherit `engc.calibration.measurement.unit` com `afr_sensor_kind_id`. Computed `sensor_kind_ids`/`measurement_unit_ids` derivados de `certificate_ids.uncertainty_lines.unit_of_measurement` nos instruments. `procedimento.item` ganhou `requires_instrument` (default por kind: foto/pdf/outro=False; excel/qualificador_data=True) + `required_sensor_kind_ids`. `collect.item` related + computed `coverage_complete`/`coverage_warning_text`/`missing_sensor_kind_ids`. Agregação no qualif + gate em `action_mark_approved` lendo nova flag `qualif_block_approval_incomplete_coverage`. Painel "Cobertura Metrológica" no form instrumento + alert vermelho coverage incompleta. 16 tests novos PASS.
- 2026-05-17 — F4.4 (16.0.3.3.1): view `<calendar>` em `afr.qualificacao.os` com `date_planned_start`/`date_planned_end` + color por `tecnico_default_id`. Action `view_mode="tree,kanban,calendar,form"`.
- 2026-05-17 — Conversão `addons/afr_qualificacao` em git submodule (repo público `github.com/afonsorcarvalho/afr_qualificacao`, branch main). Histórico 124 commits preservado via subtree split. Backup tag `pre-afr-qualificacao-submodule-conversion`. Monorepo commit `717d1c8`.
- 2026-05-17 — F4 (16.0.3.3.0): padrões metrológicos M2M `engc.calibration.instruments` em `collect.item` + agregação computed em `afr.qualificacao` (union) + `standards_all_valid`/`warning_text` baseados em `validate_calibration >= today`. Flag `qualif_block_approval_expired_standards` em `res.config.settings` (default False = warning chatter; True = `ValidationError` em `action_mark_approved`). Tab "Padrões Metrológicos" no form qualif + collect_item com alert quando expirado. 11 tests novos.
- 2026-05-17 — Trunk renomeada `feat/ciclo-report-materiais-pagina-separada` → `main-monorepo` (default branch GitHub atualizado via `gh repo edit`; master legado preservado intocado; memórias atualizadas)
- 2026-05-17 — F3 batch 16.0.3.2.1 commit 82d5578 PUSHED: tracking procedimento + chatter (F3.1), reorder stat buttons Pendentes/Coletadas + Aprovadas só visível >0 (F3.2), tree collect.items sem editable=bottom (F3.3), `ir.module.category` Qualificações + grupos com category_id (F3.4), menu raiz com grupo Técnico (F3.5), wizard descrição width full via separator (F3.6), `name_get` cycle/malha (F3.7), labels "cobertos"→"realizados" + "Cobertura técnica"→"Execução" (F3.8), domain `cycle_ids`/`malha_ids` filtra por `qualificacao_id.os_id` (F3.9). Smoke test 117/117 PASS.
- 2026-05-17 — F3 (16.0.3.2.0): procedimento template + collect.items unificados + wizard apply_procedimento (commit bba0a0d)
- 2026-05-16 — Fontes PDF +30% (parágrafos e tabelas) p/ melhor legibilidade
- 2026-05-16 — Painel HTML `qualif_subtotals_html` no form SO (subtotal por equipamento)
- 2026-05-16 — Section lines por equipamento no SO (display_type='line_section' via wizard apply)
- 2026-05-16 — PDF escopo usa `line.name` (descrição Sales) em vez de nome produto
- 2026-05-16 — Fix views: notebook+page para text/html fields (memória `feedback_odoo_text_field_width`)
- 2026-05-16 — Fix views: group nesting para renderizar label (memória `feedback_odoo_group_label`)
- 2026-05-16 — Modelo `afr.qualificacao.standard` + M2M cycle_type/malha_type + 8 normas seed (ISO 17025, FDA 21 CFR Part 11, ANVISA RDC 658/665, ISPE GAMP 5, WHO TRS 961, VIM, ABNT NBR ISO/IEC 17025)
- 2026-05-16 — Template QWeb dedicado `quotation_template.xml` (capa+sumário+descritivo+escopo+normas+resumo+condições+aceite) inherit condicional `sale.report_saleorder_document`
- 2026-05-16 — Helpers sale.order: `has_qualif_lines`, `qualif_standard_ids`, `_qualif_equipment_summary`, `_qualif_type_descriptions`
- 2026-05-16 — 13 testes novos em `test_quotation_report.py` (agregação, fallback, render PDF/HTML) — 43/43 pass
- 2026-05-16 — Bump versão 16.0.2.1.0 → 16.0.2.2.0
